# ClawNote 开发日志

## 2026-07-18：项目设计与 Agent 拆分

- 确定选题为“个人智能知识管家”。
- 将系统拆分为 steward、collector、organizer、graph、qa、recommender 六个 Agent。
- 决定由 steward 作为飞书唯一入口，其他 Agent 作为专业执行单元。
- 为避免职责交叉，明确每个 Agent 的输入、输出、异常处理和行为边界。

## 2026-07-18：飞书渠道与网络问题

- 问题：WSL 无法直连飞书和 DeepSeek，长连接和模型请求出现超时。
- 原因：WSL 直连不可用，Gateway 服务未正确使用代理；代理最初允许访问 loopback。
- 解决：为 OpenClaw 配置代理，并拒绝 `127.0.0.0/8`、`169.254.0.0/16`，代理验证通过。
- 结果：飞书机器人可以接收消息并路由到 `clawnote-steward`。

## 2026-07-18：多 Agent 调度问题

- 问题：steward 声称调用 organizer，但没有真实工具记录。
- 原因：缺少 `sessions_spawn`、`sessions_yield`、`subagents` 权限，且 Agent 出现过虚构调用。
- 解决：增加子 Agent 白名单和工具真实性规则；没有工具结果时禁止声称调用成功。
- 结果：steward 可以通过真实 Agent 调用获得 organizer 结果。

## 2026-07-19：Skill 结构调整

- 问题：原有 Skill 是平铺 Markdown，OpenClaw 无法自动识别。
- 决策：将 9 个细分说明合并为 5 个综合 Skill。
- 结构：每个业务 Agent workspace 下使用 `skills/<name>/SKILL.md`。
- 验证：`openclaw skills info <name> --agent <id>` 返回 `Ready`。

## 2026-07-19：SQLite 知识库

- 选择 SQLite，原因是零服务部署、便于演示、支持事务与 FTS5。
- 使用 `knowledge_items` 保存正文、摘要、分类、标签和来源。
- 使用 `knowledge_fts` 提供全文检索，并通过触发器同步索引。
- 使用 Python `sqlite3` 的 `?` 占位符实现参数化查询，防止 SQL 注入。

## 2026-07-19：权限与设备修复

- 问题：organizer 被 `deny: exec` 限制，无法执行数据库脚本。
- 解决：按最小权限原则仅为 organizer 开放 `read/write/exec`。
- 问题：Gateway 出现 scope upgrade pending approval。
- 解决：使用完整 Request ID 批准本机 CLI 设备权限升级。

## 2026-07-19：提交材料整理

- 更新脱敏 `config.json` 和 `permissions.json`。
- 新增确定性的文本、文件和网页采集脚本。
- 新增数据库、采集、Skill 结构和安全性自动测试。
- 补齐 README、测试报告和飞书截图清单。

## 2026-07-19：中文检索兼容

- 问题：SQLite FTS5 的 `unicode61` 对中文子串检索支持有限。
- 解决：保留 FTS5 为首选检索；零结果时使用带转义的参数化 `LIKE` 查询标题、正文、摘要和标签。
- 结果：中文关键词可以命中，返回值中的 `search_mode` 会注明 `fts5` 或 `like_fallback`。

## 2026-07-19：组员复现与路径可移植性

- 问题：Agent 指令中包含开发者专属的绝对 home 路径，其他 WSL 用户无法直接运行。
- 解决：统一改为从 Agent workspace 出发的 `../../scripts/...` 相对路径。
- 验证：增加仓库路径可移植性测试，确保提交文件不再包含用户专属 home 路径。

## 2026-07-19：课程 TypeScript Skill 规范对齐

- 差异：OpenClaw 使用 `SKILL.md` 发现能力，课程规范还要求 `index.ts`、`types.ts`、`utils.ts`、`README.md` 和独立配置。
- 决策：保留原生 `SKILL.md`，为五个业务 Skill 增加 TypeScript 可执行层，形成双层兼容结构。
- 复用：collector、organizer 和 qa 使用 `execFile` 参数数组调用既有 Python 工具，避免 shell 字符串拼接和重复实现数据库逻辑。
- 算法：graph 增加带原句证据的实体共现基线；recommender 增加标签 Jaccard 相似度、排除和稳定排序。
- 测试：新增 Jest 单元、边界、安全、性能测试及 TypeScript 到 Python 的真实采集集成测试。
- 结果：TypeScript 严格类型检查通过，6 个 Jest 套件共 19 项测试通过，Python 回归测试增至 14 项并全部通过。

## 2026-07-19：Web API CRUD 闭环

- 新增：知识详情、部分更新和删除接口，前端无需执行终端命令即可完成完整 CRUD。
- 安全：动态更新列只来自 Pydantic 固定模型，字段值与知识 ID 继续使用 SQLite 参数化绑定。
- 一致性：更新和删除操作由既有 SQLite 触发器同步 FTS5 索引。
- 资源管理：Web API 与 CLI 均在事务完成后显式关闭 SQLite 连接，避免常驻服务积累连接资源。
- 验证：先使用临时知识手动完成新增、查看、修改、删除与 404 验证，再使用临时 SQLite 数据库增加自动化生命周期测试。

## 2026-07-19：Web 智能导入

- 需求：减少手动填写标题、分类、摘要和标签的操作，允许用户直接粘贴整段知识原文。
- 流程：前端提交原文到分析接口，organizer 以 `draft` 模式返回结构化草稿；用户预览并修改后，才调用原有新增接口入库。
- 安全：正文按不可信 JSON 数据传递，提示 organizer 不执行正文内指令；后端使用无 shell 的参数数组启动 OpenClaw，并用 Pydantic 严格校验输出字段和长度。
- 隔离：分析接口不调用数据库写入工具。真实联调前后知识条目数均为 6，证明生成草稿不会自动入库。
- 验证：桌面端与 390×844 移动端布局通过；React lint、Vite 构建、21 项 Python 测试和 19 项 Jest 测试全部通过。

## 2026-07-19：多源 Web 导入

- 新增：智能导入扩展为粘贴文本、网页链接、上传文件三种入口，统一进入 organizer 草稿预览，再由用户确认入库。
- 网页：复用 collector 的 HTML 正文提取，限制 2 MiB、15 秒超时和 20000 字整理上限；保留来源 URL。
- 文件：支持 UTF-8 TXT/Markdown，限制 2 MiB，保留原始文件名作为来源。
- 安全：拒绝显式本机、内网和保留 IP；每次 HTTP 重定向重新校验目标。对 VPN 的 `198.18.0.0/15` Fake-IP 仅在检测到代理配置时放行，并依赖已验证的代理拒绝 loopback 策略。
- 验证：文件接口与 `example.com` 网页接口真实调用成功；网页到 AI 草稿的前端流程跑通，移动端三模式控件无重叠，Python 测试增至 27 项。

## 2026-07-19：AI 整理性能优化

- 现象：强化学习教程页面抓取正文约 4870 字，抓取成功但 organizer 首次整理约需 11.2 秒；开发期间 Vite 热更新还会清空尚未提交的表单，看起来像页面自动变空。
- 对照：复用常驻 Gateway 的请求超过 100 秒后超时，因此继续使用稳定的 OpenClaw `--local` 调用。
- 优化：关闭额外 thinking，约束摘要为单段精简输出，并加入 64 条基于正文和标题提示哈希的内存 LRU 草稿缓存；前端分别显示“正在抓取网页”和“AI 正在整理”。
- 结果：目标页面首次整理约 6.6 秒，相同内容再次整理约 0.002 秒；缓存随后端重启清空，不写入数据库。
- 稳定性：新增表单状态写入 `sessionStorage`，开发热更新或意外刷新后恢复导入方式、网址、原文和 AI 草稿；关闭抽屉或成功入库后自动清除。

## 2026-07-19：Web RAG 智能问答

- 架构：后端先以标题、标签、摘要和正文的加权规则检索 SQLite，再将有限证据交给 `clawnote-qa`，避免 Agent 自行重复检索或调用无关工具。
- 约束：问题和证据均作为不可信 JSON 数据传入；qa Agent 只能基于证据回答并使用 `[知识 #编号：标题]` 引用，输出由 Pydantic 严格校验。
- 防幻觉：无相关证据时直接返回无法回答，不调用模型；引用编号、标题和来源 URL 由后端生成而非相信模型输出。
- 前端：新增“智能问答”视图、对话记录、置信度、引用按钮和处理中状态；点击引用可直接打开对应知识详情。
- 实测：问题“强化学习和有监督学习的主要区别是什么？”在约 7.4 秒内返回，仅引用知识 #9；桌面端和 390×844 移动端无报错或横向溢出。

## 2026-07-19：网页公式清洗修复

- 问题：强化学习教程使用 MathJax SVG 渲染公式，其中中文被拆成多个 SVG `text` 节点，旧采集器把每个节点当作独立段落，导致“状态当前状态”等内容逐字换行。
- 解决：正文解析优先选择 `article`、`main` 或 Markdown 容器，忽略脚本、样式、导航和 SVG 内部节点，并在公式位置保留 `[数学公式]` 占位。
- 验证：目标教程重新提取为 4373 字、46 行和 3 个公式标记，不再包含逐字换行序列；数据库知识 #9 已更新为清洗后的正文。
- 回归：Python 36 项、TypeScript/Jest 19 项测试，以及 React Lint 和 Vite 生产构建全部通过。

## 2026-07-19：完整 Web 工作台与视觉升级

- 流程：在采集、organizer 入库、CRUD 和 RAG 问答基础上，补齐概览统计、知识图谱和标签相关推荐，形成五个可直接使用的 Web 视图。
- 图谱：读取 SQLite 知识、标签和已知实体，构建知识到主题关系与主题共现关系；无需模型调用，结果可重复验证。
- 推荐：以所选知识为输入，使用大小写不敏感的标签 Jaccard 相似度排序，并展示共同标签与相似度。
- 交互：新增全局“新增知识”入口、工作台统计、最近入库、分类分布、高频主题、图谱节点和推荐详情跳转。
- 视觉：统一绿色、琥珀、青色和珊瑚色语义，控制卡片圆角与信息密度；桌面端和 390px 移动端均无页面横向溢出。
- 验证：真实数据库呈现 8 条知识、32 个图谱节点和 61 条关系；Python 41 项、TypeScript/Jest 19 项测试及前端 Lint、生产构建全部通过。

## 2026-07-19：分层分类气泡图

- 问题：不同学科知识同时进入全量关系网后，跨分类连线过多，节点标题也容易互相遮挡，不适合作为答辩默认视图。
- 决策：保留原关系网络作为分析模式，新增 D3 圆形打包布局作为默认模式，按“学科分类 → 知识条目 → 标签/实体”三级逐层展开。
- 分类：展示层统一 `AI`、`AI技术`、`人工智能` 为 `AI 技术`，并将前端/后端开发归入 `软件开发`；不修改数据库原始分类。
- 交互：面包屑与返回按钮控制层级，知识层提供“查看详情”；完整标题保留在悬停提示，较小球的文字按半径动态截断和换行。
- 结果：PC 端默认展示 4 个学科球，AI 技术可展开 5 个知识球，知识可继续展开主题；全程无控制台错误，原 32 节点、61 关系网络仍可切换。
